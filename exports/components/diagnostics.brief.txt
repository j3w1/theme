j3w1/theme implementation brief — diagnostics
theme: j3w1-theme 0.1.0  profile: default  sourceDigest: sha256-7AfSaRLzDxySOSUMyoNXLzBLPUgaN3r+wiKRV9uYZJc=
pin: https://raw.githubusercontent.com/j3w1/theme/v0.1.0/exports/components/diagnostics.json
spec: spec/components/diagnostics.md  site: https://j3w1.github.io/theme/#c-diagnostics
maturity: stable  priority: R1

Summary: Inline problem markers in code (underline patterns, an error line and an overview stripe) and the problems panel that lists them as a listbox, each severity carrying its glyph.

Tokens (resolved):
  root.bg            #160b0b  ({color.surface.default})
  root.border        #a3676b  ({color.border.control})
  code.bg            #0c0909  ({color.code.bg})
  code.text          #e99499  ({color.text.default})
  code.line-number   #ad7175  ({color.code.line-number})
  code.gutter-rule   #2b0e0d  ({color.code.gutter-rule})
  code.selection-bg  #420f0c  ({color.code.selection-bg})
  error.underline    #e53935  ({color.diagnostic.error.underline})
  error.text         #f73f35  ({color.diagnostic.error.text})
  error.stripe       #e53935  ({color.diagnostic.error.stripe})
  error.line-bg      #2b0e0d  ({color.diagnostic.error.bg})
  warning.underline  #c9973f  ({color.diagnostic.warning.underline})
  warning.text       #c9973f  ({color.diagnostic.warning.text})
  warning.stripe     #c9973f  ({color.diagnostic.warning.stripe})
  info.underline     #7e9ebb  ({color.diagnostic.info.underline})
  info.text          #7e9ebb  ({color.diagnostic.info.text})
  hint.underline     #ad7175  ({color.diagnostic.hint.underline})
  hint.text          #ad7175  ({color.text.subtle})
  unused.text        #bd787d  ({color.diagnostic.unused.text})
  deprecated.text    #ad7175  ({color.code.syntax.deprecated})
  row.text           #e99499  ({color.text.default})
  row.location       #bd787d  ({color.text.muted})
  row.divider        #2b0e0d  ({color.border.divider})
  row.bg-hover       #1c0a09  ({color.interaction.hover.bg})
  row.ring           #e53935  ({color.interaction.focus.ring})
  row.bg-selected    #911410  ({color.interaction.selection.bg})
  row.text-selected  #f4eeee  ({color.interaction.selection.text})
  row.ring-selected  #ffa2a7  ({color.interaction.focus.ring-container})
  header.text        #ffa2a7  ({color.text.bright})
  status.text        #f73f35  ({color.status.danger.text})
  empty.text         #bd787d  ({color.text.muted})
  empty.glyph        #86a46f  ({color.status.success.text})

States: default, hover, focus-visible, selected, selected+focus-visible, error, empty
  default: ranges decorated as above on {color.code.bg}; the error row filled {color.diagnostic.error.bg}; panel rows on {color.surface.default} with 1px {color.border.divider} between them [underline pattern; glyph]
  hover: panel row background → {color.interaction.hover.bg}; inline ranges unchanged (the hover tooltip is a host surface) [cursor: default on rows]
  focus-visible: the active row: 1px dashed {color.interaction.focus.ring} at −2px; inline box: the container ring 2px {color.interaction.focus.ring-container} at −3px [the ring]
  selected: row filled {color.interaction.selection.bg}, text and glyph {color.interaction.selection.text}; the matching inline range filled {color.code.selection-bg} under its underline [`aria-selected`; the glyph stays; the fill spans the row]
  selected+focus-visible: the selection fill and a 1px dashed ring in {color.interaction.focus.ring-container} [both at once]
  error: the provider failed: a `role=status` line with `✕` in {color.status.danger.text} above the stale rows; the inline stripe shows the same `✕` at its top [glyph; live announcement]
  empty: no problems: rows and stripe marks hidden; `✓ No problems` in {color.text.muted} with the glyph in {color.status.success.text}; inline decorations removed [glyph; the absence of decoration]

Keyboard:
  Tab / Shift+Tab: Moves focus to and from the list as one stop; the active row is restored.
  Down / Up: Moves the active row; selection follows focus in this single-select listbox.
  Home / End: First or last row.
  Enter: Opens the location in the editor and moves focus there.
  Type-ahead: Jumps to the next row whose message starts with the typed characters.
  (host): Inline markers have no keyboard of their own; navigation between problems (F8-style) is the host's binding.

ARIA: inline: decorated spans inside a read-only textbox presentation; panel: APG listbox with aria-activedescendant (https://www.w3.org/WAI/ARIA/apg/patterns/listbox/)
Responsive: The panel fills its container; message text wraps, location and rule code stay on the row's end and never truncate the glyph; at 320px the columns stack under the message. The inline sample scrolls horizontally inside its box; the stripe stays on the inline-end edge. RTL mirrors the stripe and the location column while code stays left-to-right.
Portability: inline: spans with text-decoration inside the code-editor presentation; panel: <ul role=listbox> with <li role=option>, aria-activedescendant, aria-selected; every severity keeps its glyph in the accessible name.
  native: GTK: GtkSourceView marks and underlines (PANGO_UNDERLINE_ERROR is wavy); the panel is a GtkListView with a single-selection model.
  native: Qt: QTextCharFormat underline styles (WaveUnderline, DotLine); the panel is a QListView.
  native: JetBrains: ERRORS_ATTRIBUTES, WARNING_ATTRIBUTES, INFO_ATTRIBUTES, NOT_USED_ELEMENT_ATTRIBUTES and DEPRECATED_ATTRIBUTES in the .icls; stripe marks through the error-stripe keys.
  native: Terminal UI: underline attributes (curly where supported, else plain underline) with the glyph in the gutter; the panel is a list with the selected row inverted.

Rules: use only these values; no invented colours; radius 0; borders 1px (2px emphasis); focus is a 1px dashed ring, selection is a fill;
do not restyle unrelated elements; report deviations per agents/consume.md; record theme.lock.json at the pinned revision.
