# j3w1 theme for <Application>

Status: experimental until a real import into the recorded version is
evidenced. See `port.json` for scope and `mapping.json` for every native key.

## What it themes, and what it does not

- Supported surfaces: …
- Inherited from the host: …
- Unsupported (and why): …

## Prerequisites

Application version(s), platform, plugins.

## Install

1. Back up the existing theme settings: …
2. Download `dist/…` from the release for theme version `X.Y.Z`.
3. Import: …
4. Select the theme named `j3w1 theme` (it does not overwrite the default).
5. Restart if required.

## Verify

Open the fixture set (`templates/port/CHECKLIST.md`) and compare against the
site's specimens. Record application version, OS, font family/size, scaling,
profile, fixture revision and artifact digest in `evidence/`.

## Roll back

Select the previous theme; remove the imported file from …
