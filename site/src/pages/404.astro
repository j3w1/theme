---
import SpecLayout from "../layouts/SpecLayout.astro";
import { withBase } from "../lib/base";
---

<SpecLayout title="Not found — j3w1 UI Theme Spec" description="The page you asked for is not part of the j3w1 UI Theme Spec.">
  <header class="site-header">
    <h1>Not found</h1>
    <p class="lede">The specification is one page. <a href={withBase("")}>Open it</a>, or use its contents to find a component by anchor.</p>
  </header>
  <main id="main"></main>
</SpecLayout>
